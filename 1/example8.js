const maFunc = function(a, b) {
    return a + b;
  }
  
  // <=>
  const monArrow1 = a => a + 1;
  const monArrow2 = (a, b) => a + b;
  const monArrow3 = (a, b) => {
    const result = a + b;
    return result;
  }
  
  setTimeout(() => {
    
  })
  
  function add(a, b) {
    
  }